# requirements.txt

# --- Framework Web y Servidor ---
Flask
Flask-Cors
Flask-Limiter
gunicorn

# --- Clientes de IA ---
google-genai
pinecone

# --- Embeddings locales (debe coincidir con el modelo usado para indexar el corpus) ---
# Modelo actual: all-MiniLM-L6-v2 (384 dims, inglés)
# Migración recomendada: paraphrase-multilingual-MiniLM-L12-v2 (384 dims, multilingüe)
# — requiere re-indexar el corpus Pinecone con el nuevo modelo.
sentence-transformers

# --- RAG híbrido local (FAISS + extracción PDF) ---
faiss-cpu     # índice vectorial en RAM por sesión (cosine sobre IndexFlatIP con L2-norm)
pypdf         # extracción de texto de PDFs subidos por el usuario

# --- Utilidades ---
python-dotenv
firebase-admin

# --- Clientes de Google Cloud ---
google-cloud-bigquery

# --- Grafo de conocimiento (GraphRAG) ---
neo4j>=5.20.0

# --- Pipeline de indexación del corpus (src/corpus_pipeline.py, offline) ---
pdfplumber      # extracción de PDF con soporte de tablas y layout
python-docx     # lectura de DOCX (párrafos + tablas)
networkx        # PageRank sobre el grafo de conocimiento del corpus
