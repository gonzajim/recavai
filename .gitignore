# entorno virtual
.venv/
venv/
# variables de entorno
.env
# archivos temporales y caches
__pycache__/
*.py[cod]
.firebase/
node_modules/

# resultados del benchmark (output, se regeneran)
benchmarks/baseline.jsonl
benchmarks/run_*.jsonl

# resultados de experimentos (se regeneran)
results/

# ficheros de trabajo que no deben volver al repo
bubble_tmp.bin
temp.js
firestore-debug.log
*.ps1
.cache/

# Preguntas reales de usuarios (BigQuery): datos de usuarios, no van al repositorio.
# Se regeneran con la consulta de docs/RAG_V2_RESULTADOS.md.
data/real_queries.jsonl
data/real_queries_holdout40.jsonl
